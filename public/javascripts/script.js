"use strict";

var GroupSquare = GroupSquare = function() {

	var __construct = function() { 
		
		if (navigator.geolocation) { 
			navigator.geolocation.getPosition();
		}
	}	

	__construct();
}

GroupSquare = new GroupSquare;